# Google Drive 自動同期 実装レポート

## 変更したファイル

- `src/app/page.tsx`
  - 自動同期のトリガー、同期状態、デバウンス、同時実行防止を追加しました。
  - ローカル操作とDriveからの画面更新を区別し、同期ループを防止しました。
- `src/lib/googleDrive.ts`
  - 手動同期用の対話認証と、自動同期用の非対話認証を追加しました。
  - アクセストークン取得処理の多重実行を防止しました。
- `src/lib/sync.ts`
  - 手動／自動同期から認証方法を指定できるようにしました。
  - 既存のUUID・`updatedAt`マージと日付ソートは維持しています。
- `src/app/globals.css`
  - 同期中・同期完了・同期失敗の状態表示を追加しました。

## 同期フロー

### ローカル変更後

1. 記録追加、個別削除、JSONインポートを画面とLocalStorageへ即時反映します。
2. 最後のローカル変更から10秒待ちます。
3. 10秒以内にさらに変更された場合は、それまでのタイマーを破棄して10秒を数え直します。
4. Driveから`battle-log.json`を取得します。
5. UUID単位でマージし、同一UUIDは新しい`updatedAt`を採用します。
6. 日付順へ並べ直してLocalStorageと画面を更新します。
7. マージ結果をDriveへ保存します。

安全に競合を解決するため、自動アップロード時も既存の「取得→マージ→保存」の同期処理を利用します。

### 自動ダウンロード

次のタイミングで同じ差分同期を実行します。

- LocalStorageの初期読み込み完了後
- タブが非表示から表示へ戻ったとき
- アプリを開いている間の5分ごと

### 手動同期

既存の「Google Driveと同期」ボタンを残しています。手動・自動とも、メモリ上のアクセストークンが有効なら再利用し、期限切れの場合はまず`prompt: ""`でサイレント取得を試します。Googleセッションや認可が有効ならアカウント選択画面は表示されません。

サイレント取得に失敗した場合、自動同期は画面を突然開かず認証待ちとして終了します。ユーザーが同期ボタンを押した場合に限り、サイレント取得失敗後にアカウント選択へフォールバックします。このため、ログイン期限切れ・権限失効など再認証が必要な場合だけ選択画面が表示されます。

## 追加した状態管理

- `syncStatus`: `idle`、`syncing`、`success`、`error`の同期状態
- `syncMessage`: 状態欄へ表示するメッセージ
- `localChangeVersion`: ローカル操作のたびに増えるデバウンス用番号
- `logsRef`: 非同期処理から常に最新のログを参照するための値
- `localChangeVersionRef`: 同期中にローカル変更が発生したかを判定する値
- `syncInFlightRef`: Drive API呼び出しの多重実行防止フラグ
- `successTimerRef`: 同期完了表示を4秒後に消すタイマー

同期中に記録が追加・変更された場合は、同期開始時の変更番号と完了時の番号を比較します。差がある場合、同期結果へ最新のローカルデータを再マージし、操作中のデータが古い通信結果で上書きされることを防ぎます。

## デバウンス実装方法

ローカル変更ごとに`localChangeVersion`を更新し、Reactのeffectで10秒のタイマーを作成します。次の変更でeffectが再実行される際に直前のタイマーを解除するため、連続操作の最後から10秒後に1回だけ同期されます。

Driveから取得して`setLogs`した場合は`localChangeVersion`を変更しません。そのため、同期結果が新たな自動アップロードを発生させるループはありません。

## エラー処理と再試行

- Drive API・認証エラーは画面の同期失敗欄へ表示し、アプリ本体は継続動作します。
- エラー後も同期ロックは必ず解除されます。
- 次のローカル変更、タブ復帰、5分間隔、または手動同期で再試行できます。
- 自動同期は非対話認証を使うため、未認証時に突然認証画面を開きません。
- 手動同期も最初にサイレント取得を試し、再認証が必要な場合だけアカウント選択画面を表示します。
- 複数の同期トリガーが重なった場合、実行中の1回だけに集約します。
- 同期中にローカル変更が入り、その変更用タイマーが通信中に発火した可能性がある場合は、完了後にデバウンスを再予約します。
- Driveとマージ結果のUUID・`updatedAt`がすべて同じ場合、不要な上書きAPI呼び出しを省略します。

## 動作確認結果

- 本番ビルドとTypeScript型チェック: 成功
- ローカル変更のLocalStorage即時保存: 既存effectを維持
- 10秒デバウンス: タイマーのcleanupと変更番号依存で確認
- 起動時同期: LocalStorage読み込み完了を条件に実行
- タブ復帰同期: `visibilitychange`で`visible`の場合のみ実行
- 5分同期: 300,000ミリ秒のintervalで実行
- 手動同期: 既存ボタンから対話認証付きで実行
- 重複防止: 既存のUUID Mapマージを維持
- 競合解決: `updatedAt`比較を維持
- 日付ソート: 既存の`sortLogsByDate`を維持

実際のPC・スマートフォン間通信には、Google OAuthクライアントの設定と両端末で同じGoogleアカウントを使った実機確認が必要です。

## 既存機能への影響

- LocalStorageは引き続き正本であり、通常操作はDriveの応答を待ちません。
- 手動同期、JSONインポート／エクスポート、統計、ランキング、検索、編成保持は維持しています。
- Google Driveが未設定または一時的に利用不能でも、LocalStorageによる記録は利用できます。
- Driveからの更新では自動アップロードタイマーを開始しないため、不要なAPI呼び出しを抑制しています。
